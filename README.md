# Automated dots, a hyprland dots automated installer for arch linux written in bash.

# Installation:
  Clone the repo: ```git clone https://github.com/catowner-sudo/automated-dots.git```
  Enter the directory: ```cd automated-dots```
  Run the installer: ```chmod +x requirements.sh; bash requirements.sh```
